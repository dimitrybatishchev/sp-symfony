{% extends '::base-admin.html.twig' %}
{% block body %}
<h1>Создание статуса закупки</h1>

<form action="{{ path('admin_purchase_status_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

{% endblock %}